{# templates/profile/index.html.twig #}
{% extends 'base.html.twig' %}

{% block title %}Mon profil{% endblock %}

{% block body %}
<div class="container my-5">
    <div class="row">
        <div class="col-lg-8 mx-auto">
            <h1 class="mb-4">
                <i class="bi bi-person-circle"></i> Mon profil
            </h1>

            <div class="card shadow mb-4">
                <div class="card-body p-4">
                    <div class="row">
                        <div class="col-md-8">
                            <h3>{{ user.name }}</h3>
                            <p class="text-muted mb-3">
                                <i class="bi bi-envelope"></i> {{ user.email }}
                            </p>
                            <p class="text-muted">
                                <i class="bi bi-calendar"></i>
                                Membre depuis le {{ user.createdAt|date('d/m/Y') }}
                            </p>
                        </div>
                        <div class="col-md-4 text-md-end">
                            <div class="display-6 text-primary mb-2">{{ ideas_count }}</div>
                            <p class="text-muted">Idée{{ ideas_count > 1 ? 's' : '' }} partagée{{ ideas_count > 1 ? 's' : '' }}</p>
                        </div>
                    </div>
                </div>
            </div>

            <div class="row g-3">
                <div class="col-md-6">
                    <a href="{{ path('app_profile_edit') }}" class="btn btn-primary w-100">
                        <i class="bi bi-pencil"></i> Modifier mon profil
                    </a>
                </div>
                <div class="col-md-6">
                    <a href="{{ path('app_idea_new') }}" class="btn btn-outline-primary w-100">
                        <i class="bi bi-plus-circle"></i> Nouvelle idée
                    </a>
                </div>
            </div>

            <hr class="my-5">

            <h4 class="mb-3">Zone de danger</h4>
            <div class="card border-danger">
                <div class="card-body">
                    <h5 class="card-title text-danger">Supprimer mon compte</h5>
                    <p class="card-text">
                        Une fois votre compte supprimé, toutes vos données seront
                        définitivement effacées. Cette action est irréversible.
                    </p>
                    <button type="button"
                            class="btn btn-danger"
                            data-bs-toggle="modal"
                            data-bs-target="#deleteAccountModal">
                        <i class="bi bi-trash"></i> Supprimer mon compte
                    </button>
                </div>
            </div>
        </div>
    </div>
</div>

{# Modal de confirmation #}
<div class="modal fade" id="deleteAccountModal" tabindex="-1">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header bg-danger text-white">
                <h5 class="modal-title">Confirmer la suppression du compte</h5>
                <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal"></button>
            </div>
            <div class="modal-body">
                <p><strong>Attention !</strong> Cette action est irréversible.</p>
                <p>En supprimant votre compte :</p>
                <ul>
                    <li>Toutes vos informations personnelles seront effacées</li>
                    <li>Toutes vos idées seront supprimées</li>
                    <li>Vous ne pourrez plus accéder à votre compte</li>
                </ul>
                <p class="text-danger">Êtes-vous absolument sûr de vouloir continuer ?</p>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">
                    Non, annuler
                </button>
                <form method="post" action="{{ path('app_profile_delete') }}" class="d-inline">
                    <input type="hidden" name="_token" value="{{ csrf_token('delete_account') }}">
                    <button type="submit" class="btn btn-danger">
                        Oui, supprimer définitivement
                    </button>
                </form>
            </div>
        </div>
    </div>
</div>
{% endblock %}
